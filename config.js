import {BASE_URL} from '@env';

const config = {
  AppColor: {
    Second: '#0094ff',
    font: '#434e5a',
    primary: '#ff3b30',
    color: '#2FB105',
  },
  baseURL: BASE_URL,
  AppIcon: {},
};

export default config;
